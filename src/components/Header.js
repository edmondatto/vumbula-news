import React from 'react';

const Header = () => (
  <nav className="navbar navbar-inverse bg-primary">
    <a className="navbar-brand" id="app-logo">Vumbula News</a>
  </nav>
);

export default Header;